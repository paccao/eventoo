import { render, screen } from '@testing-library/react'
import AttendingIndicator from '../AttendingIndicator'

describe('AttendingIndicator', () => {

    it('should render', () => {
        render(<AttendingIndicator />)
    })

})